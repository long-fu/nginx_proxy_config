# 使用代理进行拉流方案

RTMP —— 推模型

```
FFmpeg ──推──> RTMP 源站
```

推流端主动发送

服务端被动接收

一条流 = 一条 TCP 连接

HTTP-FLV —— 拉模型（HTTP）

```
浏览器 ──拉──> 代理 ──拉──> 源站
```



HTTP GET 请求

谁请求，谁才建立连接

长连接持续读数据

👉 HTTP-FLV 天生是“客户端拉”

实际上发生的是👇

```sh
1️⃣ 浏览器 → 代理
   GET /live?app=live&stream=stream1

2️⃣ 代理 → 源站
   GET /live?app=live&stream=stream1

3️⃣ 源站：
   - 发现有 stream1
   - 开一条 HTTP-FLV 输出连接
   - 持续写 FLV 数据

4️⃣ 代理：
   - 边读边转发
   - 不缓存、不存盘

5️⃣ 浏览器：
   - flv.js 边收边播
```

网络真实拓扑
```
               ┌──────── 源站 A (RTMP + HTTP-FLV)
               │
浏览器 ──> 代理 ├──────── 源站 B (RTMP + HTTP-FLV)
               │
               └──────── 源站 C (RTMP + HTTP-FLV)
```

```

FFmpeg
  ├── RTMP → 源站 A
  ├── RTMP → 源站 B
  └── RTMP → 源站 C

浏览器
  └── HTTP-FLV → 代理服务器
                       ├── 拉 源站 A
                       ├── 拉 源站 B
                       └── 拉 源站 C
```

[源站配置](./src_nginx.conf)
[代理配置](./proxy_nginx.conf)


# 测试

```sh
# 推流到源站流服务器上
ffmpeg -stream_loop -1 -re -i live.mp4 \
  -c:v libx264 -c:a aac \
  -f flv \
  rtmp://192.168.8.208:1935/myapp/test

# 拉代理服务器的流
ffplay http://192.168.8.14:18090/live?src=a&port=1935&app=myapp&stream=test
```